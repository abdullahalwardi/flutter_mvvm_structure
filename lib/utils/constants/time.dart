class Time {
  const Time._();

  static const Duration small = Duration(milliseconds: 300);
  static const Duration medium = Duration(milliseconds: 600);
  static const Duration large = Duration(seconds: 1);
  static const Duration extraLarge = Duration(seconds: 2);
}
